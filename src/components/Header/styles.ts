import { createStyles } from "@material-ui/core/styles";

const styles = () => 
    createStyles({
        header: {
            padding: "2rem",
            textAlign: "center"
        }
    })

export default styles;